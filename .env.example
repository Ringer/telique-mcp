# Required: API token for Cloud Armor authentication
TELIQUE_API_TOKEN=your-token-here

# Optional: Base URL (defaults to production LB)
# TELIQUE_API_BASE_URL=https://api-dev.ringer.tel

# Optional: HTTP request timeout in milliseconds (default: 10000)
# TELIQUE_REQUEST_TIMEOUT_MS=10000
