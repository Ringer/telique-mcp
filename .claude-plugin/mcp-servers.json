{
  "mcpServers": {
    "telique-local": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "TELIQUE_API_TOKEN": "${user_config.api_token}"
      }
    }
  }
}
